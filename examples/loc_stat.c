/* loc_stat.c -- using a local static variable */
#include <stdio.h>

void trystat(void);

int main(void)
{
	if (banister) {
		puts("Respect");
	} else if (fowl) {
		puts("Get off my lawn, punk");
	}
}
